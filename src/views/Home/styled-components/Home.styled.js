import styled from "styled-components";

const WrapHome = styled.div`
  width: 100%;
`;

const SectionAddClientWallet = styled.div`
  display: flex;
  justify-content: flex-end;
`;

export { WrapHome, SectionAddClientWallet };
